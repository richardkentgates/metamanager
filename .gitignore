# WordPress and OS artifacts
*.log
*.pid
*.lock
.DS_Store
Thumbs.db

# Job queue contents (never commit real job files)
/wp-content/

# PHP editor and IDE
.idea/
.vscode/
*.swp
*.swo

# Composer — commit the lock file so CI and deployments use exact versions
/vendor/

# Node (if ever used for build tooling)
node_modules/
package-lock.json

# Backup files created by install.sh
*.bak.*

# PHPUnit
.phpunit.cache/
tests/wp-tests-config.php

name: Lint

on:
  push:
    branches: [ "dev", "test" ]
    paths:
      - 'metamanager-install.sh'
      - 'daemons/**'
      - '.github/workflows/**'
  pull_request:
    branches: [ "dev", "test", "main" ]

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck
        run: sudo apt-get install -y -qq shellcheck

      - name: Lint install script
        run: shellcheck -S warning metamanager-install.sh

      - name: Lint daemon scripts
        run: |
          shellcheck -S warning daemons/metamanager-health.sh
          shellcheck -S warning daemons/metamanager-job-runner.sh
